pi:
    cwd: /pi-cwd
    # extensions baked into the Docker image:
    extensions:
        - /app/extensions/attach.ts

# Uncomment the platform(s) you want:

# matrix:
#     homeserver: https://athena.local
#     user: "@hades:athena"
#     token: "env:MATRIX_TOKEN"
#     storage_path: /data/matrix-bot.json

discord:
    token: "env:DISCORD_TOKEN"

security:
    allowed_users:
        - "willowsparks"    # Discord username

# cron:
#     dir: /pi-cwd/cron.d
#     default_notify: "#hades:athena"    # or Discord channel ID
#     gracePeriodMs: 5000               # skip jobs if user message within last N ms (default: 5000)

# server:
#     port: 8484
#     token: "env:SERVER_TOKEN"         # Required — Bearer token for API auth
#     publicDir: /app/public            # Optional — defaults to ./public
#     host: "127.0.0.1"                 # Bind address — use "0.0.0.0" for Docker
#     trustProxy: false                 # Set true behind a reverse proxy (nginx, Caddy)

# files:
#     roots:
#         vault: /path/to/vault        # Named file roots for the workspace browser
#         sketches: /path/to/sketches  # Add as many as you like
#         cron: /path/to/cron.d

# extensions:
#     dir: /path/to/extensions     # Directory containing extension subdirectories
                                    # Each subdirectory must have a manifest.yaml
